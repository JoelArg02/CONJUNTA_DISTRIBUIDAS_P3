apiVersion: v1
kind: Service
metadata:
  name: inventory
  namespace: distribuidas-conjunta
spec:
  type: ClusterIP
  selector:
    app: inventory
  ports:
    - name: http
      port: 8082
      targetPort: 8082
