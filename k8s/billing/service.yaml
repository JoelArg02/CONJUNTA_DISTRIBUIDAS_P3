apiVersion: v1
kind: Service
metadata:
  name: billing
  namespace: distribuidas-conjunta
spec:
  type: ClusterIP
  selector:
    app: billing
  ports:
    - name: http
      port: 8080
      targetPort: 8080
